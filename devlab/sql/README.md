## Bootstrap scripts

The mysql directory contains the mysql-init.sql bootstrap script which is executed by the container during first start, which allows us to create some tables and assign the permissions as required for the Debezium CDC framework to function.